import 'package:flutter/material.dart';

class MessageRecordItem{
  Widget? playIcon;
  Widget? pauseIcon;
  TextStyle? timeStyle;
  EdgeInsets? contentPadding;

  MessageRecordItem({
    this.playIcon,
    this.pauseIcon,
    this.timeStyle,
    this.contentPadding,
  });
}