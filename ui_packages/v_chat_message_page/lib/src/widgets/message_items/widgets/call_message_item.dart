import 'package:flutter/material.dart';

class CallMessageItem extends StatelessWidget {
  const CallMessageItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
