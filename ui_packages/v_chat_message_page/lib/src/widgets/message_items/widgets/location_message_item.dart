import 'package:flutter/material.dart';

class LocationMessageItem extends StatelessWidget {
  const LocationMessageItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
