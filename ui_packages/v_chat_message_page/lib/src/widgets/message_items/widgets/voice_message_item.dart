import 'package:flutter/material.dart';
class VoiceMessageItem extends StatelessWidget {
  const VoiceMessageItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
