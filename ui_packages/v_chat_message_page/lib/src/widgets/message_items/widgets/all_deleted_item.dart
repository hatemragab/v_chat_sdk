import 'package:flutter/material.dart';

class AllDeletedItem extends StatelessWidget {
  const AllDeletedItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
