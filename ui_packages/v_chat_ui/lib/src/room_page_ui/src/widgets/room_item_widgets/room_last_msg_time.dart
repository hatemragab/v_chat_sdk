import 'package:flutter/cupertino.dart';

class RoomLastMsgTime extends StatelessWidget {
  const RoomLastMsgTime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
