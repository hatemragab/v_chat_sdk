import 'package:flutter/material.dart';

class ReplyItemWidget extends StatelessWidget {
  const ReplyItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
