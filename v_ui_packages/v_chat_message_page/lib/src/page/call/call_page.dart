library call_page;

import 'package:flutter/material.dart';
part 'call_controller.dart';

class VCallPage extends StatelessWidget {
  const VCallPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
